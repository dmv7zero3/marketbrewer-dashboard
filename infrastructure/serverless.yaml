AWSTemplateFormatVersion: "2010-09-09"
Description: "MarketBrewer Dashboard Serverless API (API Gateway + Lambda + DynamoDB + SQS)"

Parameters:
  ProjectPrefix:
    Type: String
    Default: marketbrewer
    Description: Prefix for all resources in this stack

  ClaudeApiKey:
    Type: String
    NoEcho: true
    Description: Anthropic Claude API key

  ClaudeModel:
    Type: String
    Default: claude-3-5-sonnet-20240620

  ClaudePriceInputPer1K:
    Type: Number
    Default: 0
    Description: USD cost per 1K input tokens

  ClaudePriceOutputPer1K:
    Type: Number
    Default: 0
    Description: USD cost per 1K output tokens

  ApiToken:
    Type: String
    NoEcho: true
    Description: API token for worker authentication

  ExistingQueueArn:
    Type: String
    Default: ""
    Description: Optional existing SQS queue ARN for page generation

  ExistingQueueUrl:
    Type: String
    Default: ""
    Description: Optional existing SQS queue URL for page generation

  ExistingDlqArn:
    Type: String
    Default: ""
    Description: Optional existing SQS DLQ ARN for page generation

  ExistingDlqUrl:
    Type: String
    Default: ""
    Description: Optional existing SQS DLQ URL for page generation

  ExistingTableName:
    Type: String
    Default: ""
    Description: Optional existing DynamoDB table name

  GoogleClientId:
    Type: String
    Default: ""

  GoogleAllowedEmails:
    Type: String
    Default: "j@marketbrewer.com"

  CorsAllowOrigins:
    Type: CommaDelimitedList
    Default: "https://admin.marketbrewer.com,https://portal.marketbrewer.com"

  ApiThrottleRateLimit:
    Type: Number
    Default: 25
    Description: Steady-state requests per second for API Gateway

  ApiThrottleBurstLimit:
    Type: Number
    Default: 50
    Description: Burst limit for API Gateway

  RateLimitPerMinute:
    Type: Number
    Default: 120

Conditions:
  CreateQueue: !Equals [!Ref ExistingQueueArn, ""]
  CreateTable: !Equals [!Ref ExistingTableName, ""]
  CreateDlq: !Equals [!Ref ExistingDlqArn, ""]

Resources:
  DashboardTable:
    Type: AWS::DynamoDB::Table
    Condition: CreateTable
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Sub "${ProjectPrefix}-dashboard"
      BillingMode: PAY_PER_REQUEST
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      AttributeDefinitions:
        - AttributeName: PK
          AttributeType: S
        - AttributeName: SK
          AttributeType: S
      KeySchema:
        - AttributeName: PK
          KeyType: HASH
        - AttributeName: SK
          KeyType: RANGE
      Tags:
        - Key: Project
          Value: MarketBrewer
        - Key: Component
          Value: Dashboard

  PageGenerationQueue:
    Type: AWS::SQS::Queue
    Condition: CreateQueue
    Properties:
      QueueName: !Sub "${ProjectPrefix}-page-generation"
      VisibilityTimeout: 300
      MessageRetentionPeriod: 604800
      ReceiveMessageWaitTimeSeconds: 20
      RedrivePolicy:
        deadLetterTargetArn: !If [CreateDlq, !GetAtt PageGenerationDlq.Arn, !Ref ExistingDlqArn]
        maxReceiveCount: 3
      Tags:
        - Key: Project
          Value: MarketBrewer
        - Key: Component
          Value: Dashboard

  PageGenerationDlq:
    Type: AWS::SQS::Queue
    Condition: CreateQueue
    Properties:
      QueueName: !Sub "${ProjectPrefix}-page-generation-dlq"
      MessageRetentionPeriod: 1209600
      Tags:
        - Key: Project
          Value: MarketBrewer
        - Key: Component
          Value: Dashboard

  ApiLambdaRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: "sts:AssumeRole"
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: DynamoDbAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:PutItem
                  - dynamodb:UpdateItem
                  - dynamodb:DeleteItem
                  - dynamodb:Query
                  - dynamodb:BatchWriteItem
                Resource: !If
                  - CreateTable
                  - !GetAtt DashboardTable.Arn
                  - !Sub "arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${ExistingTableName}"
        - PolicyName: SqsAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - sqs:SendMessageBatch
                Resource: !If [CreateQueue, !GetAtt PageGenerationQueue.Arn, !Ref ExistingQueueArn]

  ApiLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub "${ProjectPrefix}-dashboard-api"
      Runtime: nodejs20.x
      Handler: index.handler
      Role: !GetAtt ApiLambdaRole.Arn
      Timeout: 30
      MemorySize: 512
      Environment:
        Variables:
          DDB_TABLE_NAME: !If [CreateTable, !Ref DashboardTable, !Ref ExistingTableName]
          SQS_QUEUE_URL: !If [CreateQueue, !Ref PageGenerationQueue, !Ref ExistingQueueUrl]
          API_TOKEN: !Ref ApiToken
          GOOGLE_CLIENT_ID: !Ref GoogleClientId
          GOOGLE_ALLOWED_EMAILS: !Ref GoogleAllowedEmails
          CORS_ALLOW_ORIGINS: !Join [",", !Ref CorsAllowOrigins]
          RATE_LIMIT_PER_MIN: !Ref RateLimitPerMinute
      Code: 
        ZipFile: |
          exports.handler = async () => ({ statusCode: 503, body: "Lambda not packaged" });

  WorkerLambdaRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: "sts:AssumeRole"
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: DynamoDbAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:PutItem
                  - dynamodb:UpdateItem
                  - dynamodb:Query
                Resource: !If
                  - CreateTable
                  - !GetAtt DashboardTable.Arn
                  - !Sub "arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${ExistingTableName}"
        - PolicyName: SqsAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - sqs:ReceiveMessage
                  - sqs:DeleteMessage
                  - sqs:GetQueueAttributes
                Resource: !If [CreateQueue, !GetAtt PageGenerationQueue.Arn, !Ref ExistingQueueArn]

  WorkerLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub "${ProjectPrefix}-dashboard-worker"
      Runtime: nodejs20.x
      Handler: index.handler
      Role: !GetAtt WorkerLambdaRole.Arn
      Timeout: 60
      MemorySize: 1024
      Environment:
        Variables:
          DDB_TABLE_NAME: !If [CreateTable, !Ref DashboardTable, !Ref ExistingTableName]
          CLAUDE_API_KEY: !Ref ClaudeApiKey
          CLAUDE_MODEL: !Ref ClaudeModel
          CLAUDE_PRICE_INPUT_PER_1K: !Ref ClaudePriceInputPer1K
          CLAUDE_PRICE_OUTPUT_PER_1K: !Ref ClaudePriceOutputPer1K
      Code:
        ZipFile: |
          exports.handler = async () => ({ statusCode: 503, body: "Lambda not packaged" });

  WorkerEventSource:
    Type: AWS::Lambda::EventSourceMapping
    Properties:
      FunctionName: !GetAtt WorkerLambda.Arn
      EventSourceArn: !If [CreateQueue, !GetAtt PageGenerationQueue.Arn, !Ref ExistingQueueArn]
      BatchSize: 5
      Enabled: true

  HttpApi:
    Type: AWS::ApiGatewayV2::Api
    Properties:
      Name: !Sub "${ProjectPrefix}-dashboard-api"
      ProtocolType: HTTP
      CorsConfiguration:
        AllowOrigins: !Ref CorsAllowOrigins
        AllowHeaders:
          - "Content-Type"
          - "Authorization"
        AllowMethods:
          - GET
          - POST
          - PUT
          - DELETE
          - OPTIONS

  HttpApiIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref HttpApi
      IntegrationType: AWS_PROXY
      IntegrationUri: !GetAtt ApiLambda.Arn
      PayloadFormatVersion: "2.0"

  HttpApiRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref HttpApi
      RouteKey: "$default"
      Target: !Sub "integrations/${HttpApiIntegration}"

  HttpApiStage:
    Type: AWS::ApiGatewayV2::Stage
    Properties:
      ApiId: !Ref HttpApi
      StageName: "$default"
      AutoDeploy: true
      DefaultRouteSettings:
        ThrottlingRateLimit: !Ref ApiThrottleRateLimit
        ThrottlingBurstLimit: !Ref ApiThrottleBurstLimit
      AccessLogSettings:
        DestinationArn: !GetAtt ApiAccessLogGroup.Arn
        Format: >-
          {"requestId":"$context.requestId","httpMethod":"$context.httpMethod","path":"$context.path","status":"$context.status","latency":"$context.responseLatency","ip":"$context.identity.sourceIp","userAgent":"$context.identity.userAgent"}

  ApiAccessLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub "/aws/http-api/${ProjectPrefix}-dashboard-api"
      RetentionInDays: 30

  QueueDepthAlarm:
    Type: AWS::CloudWatch::Alarm
    Condition: CreateQueue
    Properties:
      AlarmDescription: "Page generation queue backlog"
      Namespace: "AWS/SQS"
      MetricName: "ApproximateNumberOfMessagesVisible"
      Dimensions:
        - Name: QueueName
          Value: !GetAtt PageGenerationQueue.QueueName
      Statistic: Sum
      Period: 300
      EvaluationPeriods: 1
      Threshold: 100
      ComparisonOperator: GreaterThanOrEqualToThreshold
      TreatMissingData: notBreaching

  DlqAlarm:
    Type: AWS::CloudWatch::Alarm
    Condition: CreateQueue
    Properties:
      AlarmDescription: "Page generation DLQ messages present"
      Namespace: "AWS/SQS"
      MetricName: "ApproximateNumberOfMessagesVisible"
      Dimensions:
        - Name: QueueName
          Value: !GetAtt PageGenerationDlq.QueueName
      Statistic: Sum
      Period: 300
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: GreaterThanOrEqualToThreshold
      TreatMissingData: notBreaching

  Api5xxAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmDescription: "HTTP API 5xx errors"
      Namespace: "AWS/ApiGateway"
      MetricName: "5XXError"
      Dimensions:
        - Name: ApiId
          Value: !Ref HttpApi
        - Name: Stage
          Value: "$default"
      Statistic: Sum
      Period: 300
      EvaluationPeriods: 1
      Threshold: 5
      ComparisonOperator: GreaterThanOrEqualToThreshold
      TreatMissingData: notBreaching

  ApiLatencyAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmDescription: "HTTP API latency p90 (approximate) high"
      Namespace: "AWS/ApiGateway"
      MetricName: "Latency"
      Dimensions:
        - Name: ApiId
          Value: !Ref HttpApi
        - Name: Stage
          Value: "$default"
      Statistic: Average
      Period: 300
      EvaluationPeriods: 1
      Threshold: 3000
      ComparisonOperator: GreaterThanOrEqualToThreshold
      TreatMissingData: notBreaching

  ApiLambdaPermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref ApiLambda
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub "arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${HttpApi}/*/*"

Outputs:
  ApiUrl:
    Value: !Sub "https://${HttpApi}.execute-api.${AWS::Region}.amazonaws.com"
  TableName:
    Value: !If [CreateTable, !Ref DashboardTable, !Ref ExistingTableName]
  QueueUrl:
    Value: !If [CreateQueue, !Ref PageGenerationQueue, !Ref ExistingQueueUrl]
