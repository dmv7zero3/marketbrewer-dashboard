{
  "name": "@marketbrewer/lambda-api",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.957.0",
    "@aws-sdk/lib-dynamodb": "^3.957.0",
    "@aws-sdk/client-sqs": "^3.957.0",
    "google-auth-library": "^10.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "typescript": "^5.3.0"
  }
}
